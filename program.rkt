#lang racket

(require "lexer.rkt")
(require "parser.rkt")
(require "interpreter.rkt")

; use this file as a glue for all parts


; todo complete this
(define evaluate
  (lambda (filename)
    #t))
  