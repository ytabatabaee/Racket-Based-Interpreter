#lang racket

(define pow
  (lambda (a b)
    0))

(define make-list
  (lambda (a b)
    0))

(define reverse
  (lambda (a)
    0))

(define reverse-all
  (lambda (a)
    0))

(define set
  (lambda (a index value)
    0))

(define merge
  (lambda (a b)
    0))

(define merge-sort
  (lambda (a)
    0))

(define eval
  (lambda (s)
    0))